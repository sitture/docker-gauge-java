version: 2
jobs:
  build:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run:
          name: Build image
          command: docker build --build-arg GAUGE_VERSION=0.9.7 sitture/docker-gauge-java:$CIRCLE_SHA1 .