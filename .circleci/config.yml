version: 2
jobs:
  build:
    machine:
      image: default
    steps:
      - checkout
      - run:
          name: Build Image
          command: |
            make
workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
