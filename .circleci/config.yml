version: 2
jobs:
  build:
    machine:
      docker_layer_caching: true
    working_directory: /build
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Install bash
          command: apk add --no-cache bash